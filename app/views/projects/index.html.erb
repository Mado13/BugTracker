<h1>Projects</h1>
<%= link_to "Create A Project", new_user_project_path(current_user) %>
<ul>
  <% @projects.each do |project| %>
    <li><%= link_to "#{project.title}", project_path(project.id) %></li>
  <% end %>
</ul>
